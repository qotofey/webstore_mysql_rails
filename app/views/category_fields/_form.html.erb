<%= form_with(model: category_field) do |form| %>
  <% if category_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category_field.errors.count, "error") %> prohibited this category_field from being saved:</h2>

      <ul>
        <% category_field.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type %>
  </div>

  <div class="field">
    <%= form.label :info %>
    <%= form.text_area :info %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
